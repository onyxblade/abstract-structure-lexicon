# site

詞庫的網頁版。讀 `../entries/*.md`，不寫回去。

```
npm run dev       # 開發，改 ../entries 會即時反映（網址帶 /abstract-structure-lexicon/）
npm run verify    # 檢查渲染，並列出詞庫目前的缺口
npm run build     # 靜態站 → dist/
npm run check     # TypeScript
```

## 視圖

| 路由 | 是什麼 |
| --- | --- |
| `/` | 全部詞條的一句話定義，依 family 分組 |
| `/entries/<名稱>/` | 單一詞條，`結構：` 區塊畫成圖 |
| `/build/` | 從零開始，一次加一個特徵，看還剩下哪些詞條 |
| `/traits/` | 十個特徵，一次講一個；整張矩陣收在最下面 |
| `/domains/` | 把所有例子按領域重排，看同一結構跨領域的樣子 |
| `/tensions/` | 所有核心張力，加上還沒寫張力的詞條 |

## 要改什麼，動哪裡

| 想改 | 檔案 |
| --- | --- |
| 顏色、字體、間距 | `src/styles/tokens.css` |
| 結構圖長什麼樣 | `src/styles/structure.css` |
| 結構語法怎麼被認出來 | `src/lib/structure.ts` |
| family 分組、領域分桶、停用詞、段落角色、特徵 | `src/lib/overlays.ts` |
| 段落怎麼被分到站 | `src/lib/stations.ts` |
| 特徵怎麼變成鄰接 | `src/lib/traits.ts` |
| 概念格怎麼算出來 | `src/lib/lattice.ts` |
| 詞條頁的排版 | `src/pages/entries/[name].astro` |

## 兩層資料

`src/lib/parse.ts` 跟 `src/lib/structure.ts` 只反映 `../entries` 裡真的有的東西。

`src/lib/overlays.ts` 全部是推斷出來的：詞條的 family 分組、例子領域的分桶、
概念圖的停用詞、段落標籤的角色、詞條的特徵。這些不在原始檔案裡，改它不會動到詞庫。

## 詞條頁的骨架

段落標籤是自由寫的：17 個詞條用掉 42 個不同標籤，其中 36 個只出現一次。照檔案
順序平鋪的話，`結構` 跟 `數學類比` 看起來一樣重，整頁就變成一疊。

所以每個標籤在 `src/lib/overlays.ts` 裡有一個角色，角色決定它落在哪一站。每個詞
條都走同一條七站的路，沒填到的站會留在頂端的導覽列裡，用虛線框標成缺口。

| 站 | 收哪些角色 | 目前有幾條詞條填了 |
| --- | --- | --- |
| 機制 | `mechanism` `retelling` | 17 / 17 |
| 取捨 | `tension` | 13 / 17 |
| 判準 | `criterion` | 9 / 17 |
| 拆解 | `part` | 8 / 17 |
| 旁註 | `aside` | 3 / 17 |
| 例子 | 樣板欄位 | 17 / 17 |
| 鄰接 | 樣板欄位 | 17 / 17 |

`mechanism`（只有 `結構`）跟 `tension` 是詞條的骨幹，整欄寬。其餘角色是限定用的，
排成並排的欄，因為它們彼此平行而不是接續。

新標籤沒有角色時會落到 `旁註`，`npm run verify` 會把它列出來。

## 特徵

詞條之間幾乎沒有繼承關係。把 17 個詞條兩兩對照找「A 是一種 B」，只找得到一組：
`風險記憶` 出現在 `潛勢寫入—情境實現` 的例子裡。它們共用的是零件不是祖先。

所以 `src/lib/overlays.ts` 裡宣告了十個特徵，每個是一個會重複出現的機制部件；
一個詞條就是它帶的那組特徵。`/traits/` 一次講一個特徵，整張矩陣收在頁底的摺疊區。

| 特徵 | 判斷用的問句 | 目前 |
| --- | --- | --- |
| `loop` 迴路 | 走完一圈之後，起點的值變了嗎？ | 5 / 17 |
| `weight` 權重 | 東西都在，為什麼只有一個出得來？ | 6 / 17 |
| `store` 潛伏 | 寫入和生效之間隔了多久？ | 5 / 17 |
| `recover` 可逆 | 有沒有一條路回得去？ | 4 / 17 |
| `mediate` 中介 | 拿掉中介層之後還剩下什麼？ | 5 / 17 |
| `interpreters` 多解讀器 | 只剩一個人時，這句話還成立嗎？ | 2 / 17 |
| `amplify` 放大 | 投入加一倍，輸出加幾倍？ | 4 / 17 |
| `carve` 切分 | 這條線是誰畫的，換一條會怎樣？ | 4 / 17 |
| `total` 全定義 | 起點和邊界那一格填什麼？ | 2 / 17 |
| `graded` 程度 | 問的是它有多少，還是它是不是？ | 8 / 17 |

詞條頁最下面的「鄰接」由共用的特徵決定，排序看共用幾個、以及那幾個佔兩邊簽名
的比例。在此之前它是拿 `相近概念` 的英文標籤斷詞後比對共同字，也就是兩串標籤
剛好撞到同一個字就連起來；`src/lib/graph.ts` 現在只負責決定一個概念標籤要不要
變成連結。

`npm run verify` 會檢查每個詞條至少有一個特徵、每個特徵至少有兩個詞條，
以及 `ENTRY_TRAITS` 裡沒有不存在的詞條名或沒宣告的特徵 id。

## 概念格

同一張特徵表還有另一種讀法：不要一次全部攤開，而是手上先什麼都沒有，一次加一個
特徵，看符合的詞條少掉哪些。`/build/` 就是這個，每一步一個網址，上一步用瀏覽器的
上一頁。

形式上這是對 `ENTRY_TRAITS` 做 Formal Concept Analysis。一個位置是一個 formal
concept，也就是一對（詞條集合，特徵集合），其中特徵剛好是那群詞條全都帶的，詞條
也剛好是帶齊那組特徵的全部。別的特徵組合指到的詞條，一定跟某個 concept 一樣，
只是講得比較囉唆，所以 concept 就是唯一站得住的位置。目前有 29 個位置、48 條邊，
最深 4 個特徵。

值得的地方是：一頁能給幾個選項不是我決定的，是格子決定的。起點只有七個選項而不是
十個，因為 `可逆`、`多解讀器`、`全定義` 在詞庫裡沒有單獨帶著它的詞條，只能跟著前提
一起進來。這三條蘊涵是算出來的，哪天寫進一個只有 `可逆` 沒有 `權重` 的詞條就會消失。

| 蘊涵 | 支持 |
| --- | --- |
| 可逆 ⇒ 權重 | 4 個詞條，0 反例 |
| 多解讀器 ⇒ 中介 | 2 個詞條，0 反例 |
| 全定義 ⇒ 切分 | 2 個詞條，0 反例 |

`npm run verify` 會報出位置數、邊數、一頁最多幾個選項，並在兩件事上出警告：
有特徵被全部詞條帶著（那樣 `/build/` 這個網址就沒有對應的位置），或某個詞條的
特徵組合不是一個 concept（那樣詞條頁就連不進格子裡）。

## 結構語法

`結構：` 底下的 fenced block 是一套非正式的 pseudo-code。渲染器認得這些：

| 寫法 | 畫成 |
| --- | --- |
| 行首 `→` | 直向流程，步驟間有連接線 |
| 行中 `→` | 橫向節點鏈 |
| 多行都是 `A → B` | 對齊的兩欄 |
| 在 `核心張力：` 底下的兩三行 | 左右對立的兩極卡片 |
| `A ≠ B` | 對照 |
| `A ↔ B` | 一條軸 |
| `A = B` | 公式，原樣保留 |
| `甲、乙、丙` | 一組 chip |

空行把一個 block 切成互不相干的組。認不出來的行會變成純文字，不會消失，
`npm run verify` 會確認這一點。
